<?xml version="1.0"?>
<ruleset name="Nova Checkout">
	<description>WordPress Coding Standards for Nova Checkout</description>

	<!-- What to scan -->
	<file>.</file>

	<!-- Exclude patterns -->
	<exclude-pattern>*/vendor/*</exclude-pattern>
	<exclude-pattern>*/node_modules/*</exclude-pattern>
	<exclude-pattern>*/.git/*</exclude-pattern>

	<!-- How to scan -->
	<arg value="sp"/> <!-- Show sniff and progress -->
	<arg name="colors"/>
	<arg name="extensions" value="php"/>
	<arg name="parallel" value="8"/>

	<!-- Rules: WordPress Coding Standards -->
	<rule ref="WordPress">
		<!-- Allow PSR-4 class file naming (we use autoloader) -->
		<exclude name="WordPress.Files.FileName.InvalidClassFileName"/>
		<exclude name="WordPress.Files.FileName.NotHyphenatedLowercase"/>

		<!-- Allow mixing OO and functions in main plugin file -->
		<exclude name="Universal.Files.SeparateFunctionsFromOO.Mixed"/>

		<!-- Allow reserved keywords as parameter names (WordPress uses these) -->
		<exclude name="Universal.NamingConventions.NoReservedKeywordParameterNames"/>
	</rule>

	<!-- Check for PHP cross-version compatibility -->
	<config name="testVersion" value="8.1-"/>
</ruleset>

